// Ejercicio 1: Crea un nuevo array con los cuadrados de los números
let numeros = [2, 4, 6, 8];

let cuadrados = numeros.map(num => num ** 2);

console.log("Lista original:", numeros);
console.log("Lista con cuadrados:", cuadrados);
